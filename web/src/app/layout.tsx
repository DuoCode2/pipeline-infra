export const metadata = {
  title: 'DuoCode - Business Website Demos',
  description: 'Professional website prototypes for local businesses',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
